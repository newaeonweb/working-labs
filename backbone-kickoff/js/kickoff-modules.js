define([
  'jQuery',
  'Underscore',
  'Backbone'
  // Add as many dependencies as need
], function($, _, Backbone){
	
	// Declare your Model, View, Collection, Router here
	var MyModel = Backbone.Model.extend({

	});


	// return the module(Model/View/Collection/Router)name{};
  return MyModel;
});
